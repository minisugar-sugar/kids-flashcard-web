# TODOS — Kids Flashcard Web

Auto-generated by /autoplan on 2026-03-24.

## Deferred from MVP

- [ ] **Swipe gestures** — swipe right=know, left=don't know. Deferred: tap targets sufficient for MVP, adds complexity. (Phase 2)
- [ ] **Favorite/star cards** — mark cards for extra review. Deferred: adds state complexity, borderline MVP scope. (Phase 2)
- [ ] **DESIGN.md** — create design system document after initial build. No DESIGN.md exists yet.
- [ ] **imageUrl Phase 2 limitation** — `WordCard.imageUrl` as string won't support user-uploaded images. Phase 2 needs `imageUrl | File` or IndexedDB. (Phase 2 custom word sets)
- [ ] **Dark mode** — kids apps rarely use dark mode, defer to Phase 2.
- [ ] **Spaced repetition (SM-2 / FSRS)** — replace `known/unknown` booleans with `easeFactor + nextReview` fields. (Phase 2)
- [ ] **Parent dashboard** — read progress from localStorage + display per-child stats. (Phase 2)
- [ ] **Custom word sets** — allow parents to add their own word cards. Blocked by imageUrl limitation above. (Phase 2)
- [ ] **Multi-child profiles** — separate localStorage namespaces per profile. (Phase 2)
- [ ] **Offline PWA** — service worker + manifest. (Phase 2)
- [ ] **Playwright E2E setup** — add Playwright config + E2E test directory.
